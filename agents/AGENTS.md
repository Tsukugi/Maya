# Agents Guide (Maya)

- Tests: `npm test --workspace=Maya`
- Build: `npm run build --workspace=Maya`
- Renderer components: `src/components` (ActionDiary/GameRenderer/MapRenderer).
- Keep terminal layout widths in sync with config; use `vitest` for UI snapshots/logic.
