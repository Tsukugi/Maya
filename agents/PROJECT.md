# Project Notes (Maya)

- Scope: Ink-based terminal renderer (maps, diary, layout).
- Entry: `src/index.tsx`; components under `src/components`.
- Tests: `npm test --workspace=Maya` (Vitest, v8 coverage available).
- Build: `npm run build --workspace=Maya`.
